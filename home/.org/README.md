# .org

Meant for organization specific env setup.  The root `.gitignore` excludes any subdirectories.  This could be for employers, clients, and other org specific env setup that might be considered proprietary and shouldn't be checked in to a public repository.

The `../.zsh_helpers/source-helpers.sh` script looks at all shell scripts (`*.sh`) in all subdirs in this dir and sources them.
